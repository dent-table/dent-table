import { SlotNumberTransformPipe } from './slot-number-transform.pipe';

describe('SlotNumberTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new SlotNumberTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
